import React from "react";

const Profile = (list) => {
    return(
        <div style={{alignItems:"center"}}>
            <h1 style={{color: "white"}}>This is Profile Page!!</h1>
        </div>
    )
}

export default Profile;